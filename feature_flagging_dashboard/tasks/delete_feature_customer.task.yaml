# Full reference: https://docs.airplane.dev/tasks/task-definition

slug: demo_delete_feature_customer
name: Delete feature customer
description: "SQL task that deletes a customer from a feature."
parameters:
  - slug: feature_id
    name: Feature ID
    type: integer
    description: The feature id 
    required: true
  - slug: customer_id
    name: Customer ID
    type: shorttext
    description: The customer id
    required: true
sql:
  # Replace with your own DB name
  resource: "[Demo DB]"
  entrypoint: delete_feature_customer.sql
  queryArgs:
    feature_id: "{{params.feature_id}}"
    customer_id: "{{params.customer_id}}"
