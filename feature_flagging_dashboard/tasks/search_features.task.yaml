# Full reference: https://docs.airplane.dev/tasks/task-definition

slug: demo_search_features
name: Search features
description: "SQL task that shows all feature that match a search keyword."
parameters:
  - slug: search_keyword
    name: Search keyword
    type: shorttext
    description: The search string
    required: false
sql:
  # Replace with your own DB name
  resource: "[Demo DB]"
  entrypoint: search_features.sql
  queryArgs:
    keyword: "{{params.search_keyword}}"