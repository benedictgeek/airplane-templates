# Full reference: https://docs.airplane.dev/tasks/task-definition

slug: demo_list_feature_customers
name: List customers
description: "Lists all customers with a particular feature enabled."
parameters:
  - slug: feature_id
    name: Feature ID
    type: integer
    required: false
sql:
  # Replace with your own DB name
  resource: "[Demo DB]"
  entrypoint: list_feature_customers.sql
  queryArgs:
    feature_id: "{{params.feature_id}}"
